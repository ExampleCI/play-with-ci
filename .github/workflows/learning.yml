
name: Node.js CI

on:
  push:
    branches: 
    - main

jobs:
  testing:
    runs-on: ubuntu-latest

    steps:
    -   name: Run testing
        run: echo "Run testing"

    -   name: Git clone repo
        uses: actions/checkout@v1
    -   name: Run Jest
        uses: stefanoeb/jest-action@1.0.3
   
  deploy_test:
     runs-on: ubuntu-latest 
     needs: [testing]
     steps:
     -  name: Run deploy
        run: echo "Run deploy"
